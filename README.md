
  # HACS

  This is a code bundle for HACS. The original project is available at https://www.figma.com/design/JBFxTgY6Kco4aI8IX97M59/HACS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  